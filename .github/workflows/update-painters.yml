# .github/workflows/update-painters.yml

name: Update painters.json

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install requests

      - name: Generate painters.json
        run: python generate_painters.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add painters.json

          if git diff --cached --quiet; then
            echo "No changes in painters.json"
            exit 0
          fi

          git commit -m "Auto-update painters.json"
          git pull --rebase
          git push
